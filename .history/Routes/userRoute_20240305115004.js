const express = require('express');
const router = express.Router();
const users = require('../mocks/users.json'); 



/*

router.get('/users', (req, res) => {
    res.json(users);
  });

  module.exports = router;   Example de utilisation de route*/ 
